#!/bin/sh
set -eu
[ "$#" -gt 0 ] || { echo 'usage: rig TASK...' >&2; exit 2; }
b64() { printf %s "$1" | base64 | tr -d '\n'; }
printf '__V86RPC__\tAGENT_RIG\t%s\t%s\n' "$$-$(date +%s)" "$(b64 "$*")" > /dev/ttyS0
echo '[rig] submitted to the ready browser agent.'
