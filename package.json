{
  "name": "herdr-v86",
  "version": "0.1.0",
  "private": true,
  "description": "Linux in the browser: a 32-bit v86 guest with a page-local LLM stack, browser-bridge tooling, and agent tiers.",
  "scripts": {
    "test": "node --test \"network/test/*.test.mjs\"",
    "test:agent": "npm --prefix agent test",
    "build:agent": "npm --prefix agent run build"
  }
}
